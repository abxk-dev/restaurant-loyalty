# MongoDB Atlas connection (required on Vercel + local server)
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/brewbakes?retryWrites=true&w=majority

# Local server only (Vercel sets PORT automatically)
PORT=3001
